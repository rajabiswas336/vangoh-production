# ── Server ──────────────────────────────────
PORT=5000

# ── Email (fill in your Hostinger mail details) ──
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=hello@yourdomain.com
SMTP_PASS=your_email_password

# ── Where commission emails get sent ──
RECIPIENT_EMAIL=hello@yourdomain.com
